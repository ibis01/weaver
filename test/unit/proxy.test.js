const { expect } = require("chai");

describe("Proxy Security (Unit)", () => {
  it.skip("should block internal IPs (covered in security tests)", () => {});
  it.skip("should block disallowed domains (covered in security tests)", () => {});
});
